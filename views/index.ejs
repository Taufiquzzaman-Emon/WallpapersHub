<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Wallpapers Hub</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-900 min-h-screen flex items-center justify-center font-sans p-6 text-gray-200">
  <div class="max-w-md w-full">
    <h2 class="text-2xl font-bold mb-6 text-center">All Posts</h2>

    <% posts.forEach(post => { %>
      <div class="border border-white border-opacity-20 rounded-lg p-4 mb-6 bg-white bg-opacity-10 backdrop-blur-sm">
        <h2 class="text-lg font-semibold">@<%= post.username %></h2>
        <p class="mb-3"><%= post.caption %></p>
        <img src="<%= post.image %>" alt="Uploaded Image" class="w-full rounded-md mb-3" />
        
        <div class="flex justify-between items-center mt-2">
          <a href="/posts/<%= post.id %>" class="text-blue-400 hover:underline">See Post</a>
          
          <form action="/posts/<%= post.id %>?_method=DELETE" method="POST" onsubmit="return confirm('Are you sure you want to delete this post?')">
            <button class="text-red-400 hover:text-red-600 font-medium">Delete</button>
          </form>
        </div>
      </div>
    <% }) %>

    <a href="/posts/new" class="block mt-6 text-center text-blue-400 hover:underline font-medium">Create Another Post</a>
  </div>
</body>
</html>
